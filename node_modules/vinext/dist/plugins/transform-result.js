//#region src/plugins/transform-result.ts
/** Build the standard code + sourcemap result returned by source transforms. */
function magicStringTransformResult(output, options = { hires: "boundary" }) {
	return {
		code: output.toString(),
		map: output.generateMap(options)
	};
}
//#endregion
export { magicStringTransformResult };
