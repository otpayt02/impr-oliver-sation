//#region src/plugins/environment.d.ts
type PluginEnvironment = {
  name: string;
  config: {
    consumer?: string;
  };
};
/** Whether a Vite environment produces server-consumed output. */
declare function isServerEnvironment(environment: PluginEnvironment): boolean;
//#endregion
export { isServerEnvironment };