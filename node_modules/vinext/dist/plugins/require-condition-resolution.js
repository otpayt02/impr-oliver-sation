import path from "../deps/.pnpm/pathslash@0.1.0/deps/pathslash/dist/index.js";
import { stripViteModuleQuery } from "../utils/path.js";
import { magicStringTransformResult } from "./transform-result.js";
import { SCRIPT_MODULE_ID_RE, collectBindingNames, forEachAstChild, hasRange, isAstRecord, isIdentifierNamed, nodeArray, scriptParserLanguage, staticStringValue, unwrapExpression } from "./ast-utils.js";
import { collectDirectScopeBindings, collectLoopScopeBindings, collectSwitchScopeBindings, collectVarScopeBindings, createAstScope, hasAstBinding, isFunctionNode } from "./ast-scope.js";
import { createIdResolver, parseAst } from "vite";
import { readFile } from "node:fs/promises";
import MagicString from "magic-string";
//#region src/plugins/require-condition-resolution.ts
const LITERAL_REQUIRE_RE = /\brequire\s*\(/;
const CONDITIONAL_REQUIRE_SCRIPT_ID_RE = /\.vinext-require\.(?:js|jsx|ts|tsx)$/i;
function isConditionalRequireScriptModuleId(id) {
	return CONDITIONAL_REQUIRE_SCRIPT_ID_RE.test(stripViteModuleQuery(id));
}
function syntheticModuleType(id) {
	const extension = path.extname(stripViteModuleQuery(id)).toLowerCase();
	if (extension === ".json") return "json";
	if (extension === ".jsx") return "jsx";
	if (extension === ".tsx") return "tsx";
	if (extension === ".ts" || extension === ".mts" || extension === ".cts") return "ts";
	return "js";
}
function literalString(value) {
	return staticStringValue(unwrapExpression(value));
}
function isPackageSpecifier(specifier) {
	return specifier.startsWith("#") || !specifier.startsWith(".") && !specifier.startsWith("/") && !specifier.startsWith("\\") && !/^[a-z][a-z\d+.-]*:/i.test(specifier);
}
function collectLiteralRequires(code, id) {
	let ast;
	try {
		ast = parseAst(code, { lang: scriptParserLanguage(id) ?? "jsx" });
	} catch {
		return [];
	}
	const root = isAstRecord(ast) ? ast : null;
	if (!root) return [];
	const requires = [];
	const rootScope = createAstScope(null);
	collectDirectScopeBindings(root, rootScope);
	collectVarScopeBindings(root, rootScope);
	function visit(node, parentScope) {
		let scope = parentScope;
		if (isFunctionNode(node)) {
			const parameterScope = createAstScope(parentScope);
			collectBindingNames(node.id, parameterScope.bindings);
			for (const parameter of nodeArray(node.params)) {
				collectBindingNames(parameter, parameterScope.bindings);
				if (isAstRecord(parameter)) visit(parameter, parameterScope);
			}
			const body = isAstRecord(node.body) ? node.body : null;
			if (body) {
				const bodyScope = createAstScope(parameterScope);
				collectDirectScopeBindings(body, bodyScope);
				collectVarScopeBindings(body, bodyScope);
				if (body.type === "BlockStatement") {
					for (const statement of nodeArray(body.body)) if (isAstRecord(statement)) visit(statement, bodyScope);
				} else visit(body, bodyScope);
			}
			return;
		}
		if (node.type === "SwitchStatement") {
			if (isAstRecord(node.discriminant)) visit(node.discriminant, parentScope);
			const switchScope = createAstScope(parentScope);
			collectSwitchScopeBindings(node, switchScope);
			for (const switchCase of nodeArray(node.cases)) if (isAstRecord(switchCase)) visit(switchCase, switchScope);
			return;
		}
		if (node.type === "BlockStatement" && node !== root || node.type === "StaticBlock" || node.type === "TSModuleBlock") {
			scope = createAstScope(parentScope);
			collectDirectScopeBindings(node, scope);
			if (node.type === "StaticBlock" || node.type === "TSModuleBlock") collectVarScopeBindings(node, scope);
		} else if (node.type === "CatchClause") {
			scope = createAstScope(parentScope);
			collectBindingNames(node.param, scope.bindings);
		} else if (node.type === "ForStatement" || node.type === "ForInStatement" || node.type === "ForOfStatement") {
			scope = createAstScope(parentScope);
			collectLoopScopeBindings(node, scope);
		} else if (node.type === "ClassExpression" && node.id) {
			scope = createAstScope(parentScope);
			collectBindingNames(node.id, scope.bindings);
		}
		if (node.type === "CallExpression") {
			const callee = unwrapExpression(node.callee);
			const args = nodeArray(node.arguments);
			const argument = unwrapExpression(args[0]);
			const specifier = literalString(argument);
			if (isIdentifierNamed(callee, "require") && !hasAstBinding(scope, "require") && args.length === 1 && argument && hasRange(argument) && specifier !== null && isPackageSpecifier(specifier)) {
				requires.push({
					argument,
					specifier
				});
				return;
			}
		}
		forEachAstChild(node, (child) => visit(child, scope));
	}
	for (const statement of nodeArray(root.body)) if (isAstRecord(statement)) visit(statement, rootScope);
	return requires;
}
function createRequireConditionResolutionPlugin(createResolver = createIdResolver, commonjsTransformFilter) {
	const virtualTargets = /* @__PURE__ */ new Map();
	let resolveImport;
	let resolveRequire;
	return {
		name: "vinext:require-condition-resolution",
		enforce: "pre",
		configResolved(config) {
			resolveImport = createResolver(config, { isRequire: false });
			resolveRequire = createResolver(config, { isRequire: true });
		},
		resolveId(source) {
			if (virtualTargets.has(source)) return source;
		},
		async load(id) {
			const target = virtualTargets.get(id);
			if (!target) return;
			this.addWatchFile(target.file);
			try {
				return {
					code: await readFile(target.file, "utf8"),
					moduleType: target.moduleType
				};
			} catch (error) {
				const code = typeof error === "object" && error !== null && "code" in error ? Reflect.get(error, "code") : void 0;
				if (code === "ENOENT" || code === "EISDIR") return;
				throw error;
			}
		},
		transform: {
			filter: {
				id: SCRIPT_MODULE_ID_RE,
				code: LITERAL_REQUIRE_RE
			},
			async handler(code, id) {
				const cleanId = stripViteModuleQuery(id);
				const commonjsDisposition = commonjsTransformFilter?.(cleanId);
				if (commonjsDisposition === false || commonjsDisposition !== true && cleanId.includes("node_modules")) return null;
				const requires = collectLiteralRequires(code, id);
				if (requires.length === 0 || !resolveImport || !resolveRequire) return null;
				const output = new MagicString(code);
				let changed = false;
				for (const { argument, specifier } of requires) {
					const [requireResolution, importResolution] = await Promise.all([resolveRequire(this.environment, specifier, id), resolveImport(this.environment, specifier, id)]);
					if (!requireResolution || requireResolution === specifier || !path.isAbsolute(stripViteModuleQuery(requireResolution))) continue;
					const requirePath = stripViteModuleQuery(requireResolution);
					if (requirePath === (importResolution ? stripViteModuleQuery(importResolution) : importResolution)) continue;
					const moduleType = syntheticModuleType(requirePath);
					const virtualId = `${requirePath}.vinext-require.${moduleType}`;
					virtualTargets.set(virtualId, {
						file: requirePath,
						moduleType
					});
					output.overwrite(argument.start, argument.end, JSON.stringify(virtualId));
					changed = true;
				}
				if (!changed) return null;
				return magicStringTransformResult(output, {
					hires: "boundary",
					source: id
				});
			}
		}
	};
}
//#endregion
export { createRequireConditionResolutionPlugin, isConditionalRequireScriptModuleId };
