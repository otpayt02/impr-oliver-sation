//#region src/cache/cache-adapters-virtual.d.ts
/**
 * A serializable pointer to a cache adapter module — the shape of each `cache`
 * slot in the vinext() plugin config. Produced by an adapter builder (e.g.
 * `kvDataAdapter(...)` from `@vinext/cloudflare/cache/kv-data-adapter`) or written
 * by hand. `options` must be JSON-serializable: it is inlined into the generated
 * registration module and forwarded to the adapter factory at runtime.
 */
type CdnCacheAdapterCapabilities = {
  /**
   * Page responses include the current application build identity in the
   * framework-owned `X-Vinext-Build-Id` response header, including responses
   * that are ultimately marked non-cacheable.
   *
   * Deploy adapters use this guarantee to distinguish the newly uploaded
   * Worker from an older version while staged traffic is propagating.
   */
  buildIdentity?: "response-header";
  /**
   * The shared cache selects response variants using every request header
   * named by `Vary`, comparing the header values verbatim.
   *
   * Vinext only uses the canonical deploy-warmed RSC request shape when this
   * guarantee is present. URL-only caches retain the contextual `_rsc` digest.
   */
  responseVary?: "verbatim";
  /**
   * Cacheable App Page responses require a build-bound probe manifest before
   * the adapter may emit public CDN cache policy. Cloudflare deploy tooling
   * carries that manifest as a module in a second Worker version.
   */
  routeCacheability?: "probe-manifest";
};
type CacheAdapterBuildOutput = {
  /** Whether this adapter owns output for the resolved build platform. */
  matchesBuild?: (build: {
    plugins: readonly {
      name?: string;
    }[];
  }) => boolean;
  /** Finalize an emitted directory after other platform output hooks. */
  finalizeBuildOutput?: (output: {
    root: string;
    outDir: string;
    isPrimaryServerOutput: boolean;
  }) => Promise<void> | void;
};
type CacheAdapterDescriptor<O extends Record<string, unknown> = Record<string, unknown>> = {
  /**
   * Module specifier (or absolute path, e.g. from `require.resolve(...)`) whose
   * default export is a cache adapter factory.
   */
  adapter: string;
  /** JSON-serializable options forwarded to the factory at runtime. */
  options?: O;
  /** Optional adapter-owned finalization for platform-generated build output. */
  output?: CacheAdapterBuildOutput;
  /** Build-time cache semantics used by shared request protocol code. */
  capabilities?: CdnCacheAdapterCapabilities;
};
declare function hasVerbatimResponseVary(cache?: VinextCacheConfig | null): boolean;
declare function hasBuildIdentityResponseHeader(cache?: VinextCacheConfig | null): boolean;
declare function requiresRouteCacheabilityProbeManifest(cache?: VinextCacheConfig | null): boolean;
/**
 * The `cache` option of the vinext() plugin: declaratively register cache
 * handlers instead of calling `setDataCacheHandler()` / `setCdnCacheAdapter()`
 * from a worker entry.
 */
type VinextCacheConfig = {
  /** Page-level ISR serving strategy (CDN cache adapter). */
  cdn?: CacheAdapterDescriptor;
  /** Data cache (fetch / `"use cache"` / `unstable_cache`) handler. */
  data?: CacheAdapterDescriptor;
};
/** Public virtual module id imported by the server entries. */
declare const VIRTUAL_CACHE_ADAPTERS = "virtual:vinext-cache-adapters";
declare const VINEXT_CACHE_CONFIG_PLUGIN_PROPERTY = "__vinextCacheConfig";
type ViteConfigLoader = {
  loadConfigFromFile: typeof import("vite").loadConfigFromFile;
};
declare function findVinextCacheConfigInPlugins(plugins: import("vite").PluginOption[] | undefined): Promise<VinextCacheConfig | null>;
declare function loadVinextCacheConfigFromViteConfig(vite: ViteConfigLoader, root: string): Promise<VinextCacheConfig | null>;
/**
 * Generate the source of the `virtual:vinext-cache-adapters` module for the
 * given config. Always exports `registerConfiguredCacheAdapters(env)`.
 */
declare function generateCacheAdaptersModule(cache?: VinextCacheConfig): string;
//#endregion
export { CacheAdapterBuildOutput, CacheAdapterDescriptor, CdnCacheAdapterCapabilities, VINEXT_CACHE_CONFIG_PLUGIN_PROPERTY, VIRTUAL_CACHE_ADAPTERS, VinextCacheConfig, findVinextCacheConfigInPlugins, generateCacheAdaptersModule, hasBuildIdentityResponseHeader, hasVerbatimResponseVary, loadVinextCacheConfigFromViteConfig, requiresRouteCacheabilityProbeManifest };