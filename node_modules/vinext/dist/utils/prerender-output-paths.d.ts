//#region src/utils/prerender-output-paths.d.ts
/**
 * Determine the HTML output file path for a prerendered URL.
 * Respects trailingSlash config.
 */
declare function getOutputPath(urlPath: string, trailingSlash: boolean, basePath?: string): string;
/** Determine the Flight payload path for a prerendered App Router URL. */
declare function getRscOutputPath(urlPath: string, options?: {
  mode: "default" | "export";
  trailingSlash: boolean;
  basePath?: string;
}): string;
/** Determine the binary artifact path for a prerendered App Route response. */
declare function getAppRouteOutputPath(urlPath: string): string;
//#endregion
export { getAppRouteOutputPath, getOutputPath, getRscOutputPath };