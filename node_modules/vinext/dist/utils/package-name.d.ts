//#region src/utils/package-name.d.ts
declare const BARE_PACKAGE_SPECIFIER_RE: RegExp;
/**
 * Extract the npm package name from a bare package specifier.
 *
 * Relative and absolute paths, package imports, URL/virtual schemes, malformed
 * scoped names, and Node builtins are not npm package references.
 */
declare function packageNameFromSpecifier(specifier: string): string | null;
//#endregion
export { BARE_PACKAGE_SPECIFIER_RE, packageNameFromSpecifier };