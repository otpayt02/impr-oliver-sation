import { NextI18nConfig } from "../config/next-config.js";
import { MiddlewareMatcherObject } from "./middleware-matcher-pattern.js";
//#region src/server/middleware-matcher.d.ts
type MatcherConfig = string | Array<string | MiddlewareMatcherObject>;
type MiddlewareLocaleMatchContext = {
  kind: "defaulted";
  defaultLocale: string;
} | {
  kind: "internal" | "literal";
};
declare function matchesMiddleware(pathname: string, matcher: MatcherConfig | undefined, request?: Request, i18nConfig?: NextI18nConfig | null, localeContext?: MiddlewareLocaleMatchContext): boolean;
/**
 * Whether a pathname can reach middleware, ignoring request-specific
 * `has`/`missing` conditions. This is deliberately separate from
 * `matchesMiddleware`: ordinary middleware execution must still evaluate the
 * full request, while cacheability certification needs a stable answer for
 * every request that can share the pathname's cache entry.
 */
declare function matchesMiddlewarePathname(pathname: string, matcher: MatcherConfig | undefined, i18nConfig?: NextI18nConfig | null, localeContext?: MiddlewareLocaleMatchContext): boolean;
declare function matchPattern(pathname: string, pattern: string): boolean;
//#endregion
export { MatcherConfig, MiddlewareLocaleMatchContext, matchPattern, matchesMiddleware, matchesMiddlewarePathname };