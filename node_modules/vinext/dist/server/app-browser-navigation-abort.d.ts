//#region src/server/app-browser-navigation-abort.d.ts
type AppBrowserNavigationAbortHandle = {
  release(): void;
  signal: AbortSignal;
};
/**
 * Owns cancellation only while a navigation still has abortable transport work.
 * Once a Flight response is handed to React, release the handle so a newer
 * navigation does not abort the response body while React is decoding it.
 */
declare function createAppBrowserNavigationAbortCoordinator(): {
  abortActive(): void;
  begin(): AppBrowserNavigationAbortHandle;
};
//#endregion
export { AppBrowserNavigationAbortHandle, createAppBrowserNavigationAbortCoordinator };