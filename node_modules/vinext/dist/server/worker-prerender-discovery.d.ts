import { ExecutionContextLike } from "../shims/request-context.js";
//#region src/server/worker-prerender-discovery.d.ts
declare function isWorkerPrerenderDiscoveryPath(pathname: string): boolean;
declare function workerCapabilityMatches(provided: string, expected: string): boolean;
/**
 * Respond to an authenticated staged-version readiness request without
 * invoking middleware, routing, or rendering. The CDN adapter validates the
 * expected Worker version before callers return this response.
 */
declare function createWorkerPrerenderReadinessResponse(ctx: ExecutionContextLike, request: Request): Response | null;
/**
 * Mark only a request carrying the current build's capability as an internal
 * path-discovery request. The capability is compiled into the Worker entry and
 * rotated on every build; the public version override proves which staged
 * version Cloudflare invoked but is deliberately not treated as authorization.
 */
declare function createWorkerPrerenderDiscoveryContext(base: ExecutionContextLike, request: Request, expectedSecret: string | null | undefined): ExecutionContextLike;
//#endregion
export { createWorkerPrerenderDiscoveryContext, createWorkerPrerenderReadinessResponse, isWorkerPrerenderDiscoveryPath, workerCapabilityMatches };