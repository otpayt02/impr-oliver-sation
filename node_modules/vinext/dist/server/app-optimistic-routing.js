import { buildParams, decodeMatchedParams, splitPathnameForRouteMatch } from "../routing/utils.js";
import { stripBasePath } from "../utils/base-path.js";
import { isUnknownRecord } from "../utils/record.js";
import { AppElementsWire } from "./app-elements-wire.js";
import "./app-elements.js";
import { extractRawRoutePatternParams, matchRoutePattern } from "../routing/route-pattern.js";
import { createNestedBfcacheSlotSegmentId, deriveBfcacheSegmentIdentity, isNestedBfcacheSlotSegmentId } from "./bfcache-identity.js";
import { stripRscCacheBustingSearchParam, stripRscSuffix } from "./app-rsc-cache-busting.js";
import { canonicalizeAppPageParams, resolveAppPagePatternStateKey, resolveAppPageSemanticSegmentStateKey } from "./app-page-segment-state.js";
import { Suspense, createElement, isValidElement } from "react";
//#region src/server/app-optimistic-routing.ts
const routeTrieCache = /* @__PURE__ */ new WeakMap();
const OPTIMISTIC_ROUTE_SEGMENT_SUSPENSE_TRIGGER = new Promise(() => {});
function getOptimisticRouteTemplateKey(options) {
	return `${options.routeId}\0${options.interceptionContext ?? ""}\0${options.mountedSlotsHeader ?? ""}`;
}
function getOptimisticPrefetchSourceKey(options) {
	return `${options.cacheKey}\0${options.interceptionContext ?? ""}\0${options.mountedSlotsHeader ?? ""}`;
}
function createNode() {
	return {
		catchAllChild: null,
		dynamicChild: null,
		optionalCatchAllChild: null,
		route: null,
		staticChildren: /* @__PURE__ */ new Map()
	};
}
function buildRouteTrie(routeManifest) {
	const root = createNode();
	for (const route of routeManifest.segmentGraph.routes.values()) {
		let node = root;
		const parts = route.patternParts;
		if (parts.length === 0) {
			node.route ??= route;
			continue;
		}
		for (const [index, part] of parts.entries()) {
			const isTerminal = index === parts.length - 1;
			if (part.startsWith(":") && part.endsWith("+")) {
				if (isTerminal && node.catchAllChild === null) node.catchAllChild = {
					paramName: part.slice(1, -1),
					route
				};
				break;
			}
			if (part.startsWith(":") && part.endsWith("*")) {
				if (isTerminal && node.optionalCatchAllChild === null) node.optionalCatchAllChild = {
					paramName: part.slice(1, -1),
					route
				};
				break;
			}
			if (part.startsWith(":")) {
				const paramName = part.slice(1);
				if (node.dynamicChild === null) node.dynamicChild = {
					node: createNode(),
					paramName
				};
				else if (node.dynamicChild.paramName !== paramName && import.meta.env.DEV) console.warn(`[vinext] Optimistic route trie found conflicting dynamic segments at the same level: :${node.dynamicChild.paramName} vs ${part}`);
				node = node.dynamicChild.node;
				if (isTerminal) node.route ??= route;
				continue;
			}
			let staticChild = node.staticChildren.get(part);
			if (staticChild === void 0) {
				staticChild = createNode();
				node.staticChildren.set(part, staticChild);
			}
			node = staticChild;
			if (isTerminal) node.route ??= route;
		}
	}
	return root;
}
function getRouteTrie(routeManifest) {
	const existing = routeTrieCache.get(routeManifest);
	if (existing) return existing;
	const trie = buildRouteTrie(routeManifest);
	routeTrieCache.set(routeManifest, trie);
	return trie;
}
function matchNode(node, urlParts, index, entries) {
	if (index === urlParts.length) {
		if (node.route !== null) return {
			route: node.route,
			params: buildParams(entries)
		};
		if (node.optionalCatchAllChild !== null) return {
			route: node.optionalCatchAllChild.route,
			params: buildParams(entries)
		};
		return null;
	}
	const segment = urlParts[index];
	const staticChild = node.staticChildren.get(segment);
	if (staticChild !== void 0) return matchNode(staticChild, urlParts, index + 1, entries);
	if (node.dynamicChild !== null) {
		entries.push([node.dynamicChild.paramName, segment]);
		const match = matchNode(node.dynamicChild.node, urlParts, index + 1, entries);
		if (match !== null) return match;
		entries.pop();
	}
	if (node.catchAllChild !== null) {
		const params = buildParams(entries);
		params[node.catchAllChild.paramName] = urlParts.slice(index);
		return {
			route: node.catchAllChild.route,
			params
		};
	}
	if (node.optionalCatchAllChild !== null) {
		const params = buildParams(entries);
		params[node.optionalCatchAllChild.paramName] = urlParts.slice(index);
		return {
			route: node.optionalCatchAllChild.route,
			params
		};
	}
	return null;
}
function hrefToRouteParts(href, basePath) {
	let url;
	try {
		url = new URL(href, "https://vinext.local");
	} catch {
		return null;
	}
	stripRscCacheBustingSearchParam(url);
	const appPathname = stripBasePath(stripRscSuffix(url.pathname), basePath);
	const pathname = appPathname === "" ? "/" : appPathname;
	return {
		normalized: splitPathnameForRouteMatch(pathname),
		raw: pathname.split("/").filter(Boolean)
	};
}
function matchOptimisticRouteManifestRoute(options) {
	const urlParts = hrefToRouteParts(options.href, options.basePath);
	if (urlParts === null) return null;
	const match = matchNode(getRouteTrie(options.routeManifest), urlParts.normalized, 0, []);
	if (match === null) return null;
	decodeMatchedParams(match.params);
	return match;
}
function mergeParams(target, source) {
	for (const [key, value] of Object.entries(source)) target[key] = value;
}
function resolveOptimisticNavigationParams(options) {
	const routeParams = extractRawRoutePatternParams(options.match.route.patternParts, options.rawUrlParts);
	canonicalizeAppPageParams(routeParams);
	const navigationParams = { ...routeParams };
	const routeParamNames = new Set(options.match.route.paramNames);
	for (const binding of options.routeManifest.segmentGraph.slotBindings.values()) {
		if (binding.routeId !== options.match.route.id || binding.state !== "active") continue;
		const patternParts = binding.slotPatternParts;
		if (!patternParts) continue;
		if (binding.slotParamNames?.every((name) => routeParamNames.has(name))) continue;
		const matched = matchRoutePattern(options.urlParts, patternParts);
		if (matched) mergeParams(navigationParams, matched);
	}
	return {
		navigationParams,
		routeParams
	};
}
function resolveOptimisticNavigationParamsForHref(options) {
	const urlParts = hrefToRouteParts(options.href, options.basePath);
	if (urlParts === null) return null;
	const match = matchOptimisticRouteManifestRoute(options);
	if (match === null) return null;
	return resolveOptimisticNavigationParams({
		match,
		rawUrlParts: urlParts.raw,
		routeManifest: options.routeManifest,
		urlParts: urlParts.normalized
	}).navigationParams;
}
function collectRenderedBfcacheSegmentIds(value, candidates, rendered, depth = 0) {
	if (depth > 100) return;
	if (Array.isArray(value)) {
		for (const entry of value) collectRenderedBfcacheSegmentIds(entry, candidates, rendered, depth + 1);
		return;
	}
	if (!isValidElement(value)) return;
	const props = Reflect.get(value, "props");
	if (!isUnknownRecord(props)) return;
	const id = Reflect.get(props, "id");
	if (typeof id === "string" && candidates.has(id)) rendered.add(id);
	for (const prop of Object.values(props)) collectRenderedBfcacheSegmentIds(prop, candidates, rendered, depth + 1);
}
function getOmittedBfcacheSegmentIds(elements) {
	const identities = AppElementsWire.readMetadata(elements).bfcacheSegmentIdentities;
	const candidates = new Set(Object.keys(identities).filter((id) => isNestedBfcacheSlotSegmentId(id) && Object.hasOwn(elements, id)));
	if (candidates.size === 0) return [];
	const rendered = /* @__PURE__ */ new Set();
	for (const value of Object.values(elements)) collectRenderedBfcacheSegmentIds(value, candidates, rendered);
	return [...candidates].filter((id) => !rendered.has(id));
}
function resolveTargetBfcacheSegmentIdentity(options) {
	const route = options.routeManifest.segmentGraph.routes.get(options.routeId);
	if (!route) return void 0;
	const routeParamNames = new Set(route.paramNames);
	for (const binding of options.routeManifest.segmentGraph.slotBindings.values()) {
		if (binding.routeId !== options.routeId) continue;
		const segmentParams = binding.state === "active" && binding.slotPatternParts !== void 0 && binding.slotPatternParts.length > 0 && !binding.slotParamNames?.every((name) => routeParamNames.has(name)) ? matchRoutePattern(options.targetUrlParts, binding.slotPatternParts) ?? options.targetRouteParams : options.targetRouteParams;
		const ownerLayout = binding.ownerLayoutId ? options.routeManifest.segmentGraph.layouts.get(binding.ownerLayoutId) : void 0;
		const ownerStateKey = ownerLayout ? resolveAppPagePatternStateKey(ownerLayout.patternParts, options.targetRouteParams) : "";
		const boundSegmentKeys = [];
		let level = 0;
		for (const segment of binding.routeSegments ?? []) {
			if (segment.startsWith("@")) continue;
			level += 1;
			boundSegmentKeys.push(resolveAppPageSemanticSegmentStateKey({
				marker: null,
				paramSource: "slot",
				segment
			}, segmentParams));
			if (createNestedBfcacheSlotSegmentId(binding.slotId, level) !== options.segmentId) continue;
			const identityKey = JSON.stringify(boundSegmentKeys);
			return deriveBfcacheSegmentIdentity({
				activeRouteGraphId: null,
				boundSegmentKey: ownerStateKey ? JSON.stringify([ownerStateKey, identityKey]) : identityKey,
				interceptionTargetRouteGraphId: null,
				kind: "slot",
				ownerLayoutGraphId: binding.ownerLayoutId,
				slotGraphId: binding.slotId,
				state: binding.state
			});
		}
		if (level === 0 && createNestedBfcacheSlotSegmentId(binding.slotId, 1) === options.segmentId) return deriveBfcacheSegmentIdentity({
			activeRouteGraphId: null,
			boundSegmentKey: ownerStateKey ? JSON.stringify([ownerStateKey, ""]) : "",
			interceptionTargetRouteGraphId: null,
			kind: "slot",
			ownerLayoutGraphId: binding.ownerLayoutId,
			slotGraphId: binding.slotId,
			state: binding.state
		});
	}
}
function elementHasSuspenseFallback(value, depth = 0) {
	if (depth > 100) return false;
	if (Array.isArray(value)) return value.some((entry) => elementHasSuspenseFallback(entry, depth + 1));
	if (!isValidElement(value)) return false;
	const props = Reflect.get(value, "props");
	if (value.type === Suspense && isUnknownRecord(props)) {
		const fallback = Reflect.get(props, "fallback");
		if (fallback !== null && fallback !== void 0) return true;
	}
	if (!isUnknownRecord(props)) return false;
	return elementHasSuspenseFallback(Reflect.get(props, "children"), depth + 1);
}
function getPageElementIds(elements, route) {
	const pageElementIds = /* @__PURE__ */ new Set();
	if (route.pageId && Object.hasOwn(elements, route.pageId)) pageElementIds.add(route.pageId);
	for (const slotId of route.slotIds) {
		const parsed = AppElementsWire.parseElementKey(slotId);
		if (parsed?.kind === "slot" && parsed.name === "children" && Object.hasOwn(elements, slotId)) pageElementIds.add(slotId);
	}
	for (const key of Object.keys(elements)) if (AppElementsWire.parseElementKey(key)?.kind === "page") pageElementIds.add(key);
	return Array.from(pageElementIds).sort();
}
function OptimisticRouteSegment() {
	throw OPTIMISTIC_ROUTE_SEGMENT_SUSPENSE_TRIGGER;
}
function createOptimisticRouteTemplate(options) {
	const match = matchOptimisticRouteManifestRoute({
		basePath: options.basePath,
		href: options.href,
		routeManifest: options.routeManifest
	});
	if (match === null || !options.allowLoadingShell && !match.route.isDynamic) return null;
	if (options.interceptionContext !== null) return null;
	const metadata = AppElementsWire.readMetadata(options.elements);
	if (metadata.interception !== null || metadata.interceptionContext !== null) return null;
	const routeElement = options.elements[metadata.routeId];
	if (!options.allowLoadingShell && !elementHasSuspenseFallback(routeElement)) return null;
	if (options.allowLoadingShell && options.elements["__prefetchLoadingShell"] !== "LoadingBoundary") return null;
	if (options.allowLoadingShell && (routeElement === void 0 || routeElement === null)) return null;
	const pageElementIds = getPageElementIds(options.elements, match.route);
	if (pageElementIds.length === 0) return null;
	return {
		elements: options.elements,
		mountedSlotsHeader: options.mountedSlotsHeader,
		omittedBfcacheSegmentIds: options.elements["__prefetchLoadingShell"] === "LoadingBoundary" ? getOmittedBfcacheSegmentIds(options.elements) : [],
		omittedLayoutIds: options.elements["__prefetchLoadingShell"] === "LoadingBoundary" ? metadata.layoutIds.filter((layoutId) => !Object.hasOwn(options.elements, layoutId)) : [],
		pageElementIds,
		routeId: match.route.id
	};
}
function createOptimisticRouteElements(template) {
	const elements = { ...template.elements };
	for (const pageElementId of template.pageElementIds) elements[pageElementId] = createElement(OptimisticRouteSegment);
	return elements;
}
/**
* A loading-shell prefetch stops at the first loading boundary, so layouts
* below that boundary are present in the route metadata but absent from the
* rendered shell. Do not commit that ancestor fallback when one of those
* omitted layouts is already mounted with the same semantic identity. Next.js
* keeps the shared segment active in this case, which also means the ancestor
* loading boundary does not re-trigger.
*/
function canCommitOptimisticRouteTemplate(options) {
	if (options.template.omittedLayoutIds.length === 0 && options.template.omittedBfcacheSegmentIds.length === 0) return true;
	const currentLayoutIds = new Set(options.currentLayoutIds);
	const currentIdentities = AppElementsWire.readMetadata(options.currentElements).bfcacheSegmentIdentities;
	for (const layoutId of options.template.omittedLayoutIds) {
		if (!currentLayoutIds.has(layoutId)) continue;
		if (!Object.hasOwn(options.currentElements, layoutId)) continue;
		const layout = options.routeManifest.segmentGraph.layouts.get(layoutId);
		if (layout === void 0) continue;
		const currentIdentity = currentIdentities[layoutId];
		if (currentIdentity !== void 0) {
			if (currentIdentity === deriveBfcacheSegmentIdentity({
				boundSegmentKey: resolveAppPagePatternStateKey(layout.patternParts, options.targetRouteParams),
				graphId: layout.id,
				kind: "layout",
				rootBoundaryId: layout.rootBoundaryId
			})) return false;
			continue;
		}
		if (resolveAppPagePatternStateKey(layout.patternParts, options.currentParams) === resolveAppPagePatternStateKey(layout.patternParts, options.targetRouteParams)) return false;
	}
	for (const segmentId of options.template.omittedBfcacheSegmentIds) {
		if (!Object.hasOwn(options.currentElements, segmentId)) continue;
		const currentIdentity = currentIdentities[segmentId];
		const targetIdentity = resolveTargetBfcacheSegmentIdentity({
			routeId: options.template.routeId,
			routeManifest: options.routeManifest,
			segmentId,
			targetRouteParams: options.targetRouteParams,
			targetUrlParts: options.targetUrlParts
		});
		if (currentIdentity !== void 0 && targetIdentity !== void 0 && currentIdentity === targetIdentity) return false;
	}
	return true;
}
function resolveOptimisticNavigationPayload(options) {
	if (options.interceptionContext !== null) return null;
	const urlParts = hrefToRouteParts(options.href, options.basePath);
	if (urlParts === null) return null;
	const match = matchOptimisticRouteManifestRoute({
		basePath: options.basePath,
		href: options.href,
		routeManifest: options.routeManifest
	});
	if (match === null) return null;
	const template = options.templates.get(getOptimisticRouteTemplateKey({
		interceptionContext: options.interceptionContext,
		mountedSlotsHeader: options.mountedSlotsHeader,
		routeId: match.route.id
	}));
	if (template === void 0) return null;
	if (template.mountedSlotsHeader !== options.mountedSlotsHeader) return null;
	const { navigationParams, routeParams } = resolveOptimisticNavigationParams({
		match,
		rawUrlParts: urlParts.raw,
		routeManifest: options.routeManifest,
		urlParts: urlParts.normalized
	});
	return {
		elements: createOptimisticRouteElements(template),
		params: navigationParams,
		routeParams,
		template,
		urlParts: urlParts.normalized
	};
}
//#endregion
export { canCommitOptimisticRouteTemplate, createOptimisticRouteElements, createOptimisticRouteTemplate, getOptimisticPrefetchSourceKey, getOptimisticRouteTemplateKey, matchOptimisticRouteManifestRoute, resolveOptimisticNavigationParamsForHref, resolveOptimisticNavigationPayload };
