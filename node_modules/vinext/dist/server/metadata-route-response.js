import { closeAfterResponse, createRequestContext, markFullyBufferedBody, runWithRequestContext } from "../shims/unified-request-context.js";
import { getRequestExecutionContext } from "../shims/request-context.js";
import { VINEXT_METADATA_ROUTE_CACHE_HEADER } from "./headers.js";
import { notFoundResponse } from "./http-error-responses.js";
import { resolveClientStaleTimeSeconds } from "../utils/cache-control-metadata.js";
import { isrCacheControl } from "./isr-cache.js";
import { _consumeRequestScopedCacheLife, cacheLifeProfiles } from "../shims/cache-request-state.js";
import { ensureFetchPatch, getCollectedFetchTags, setCurrentFetchSoftTags } from "../shims/fetch-cache.js";
import { isValidMetadataImageId, manifestToJson, matchMetadataRoutePattern, robotsToText, sitemapToXml } from "./metadata-routes.js";
import { applyPrerenderCacheLifeHeader, applyPrerenderCacheTagsHeader } from "./prerender-cache-life-header.js";
import { buildPageCacheTags } from "./implicit-tags.js";
import { buildAppRouteCacheValue, buildRouteHandlerCachedResponse } from "./app-route-handler-response.js";
import { isMetadataResponseCacheable } from "./metadata-route-cache-policy.js";
//#region src/server/metadata-route-response.ts
function isOuterMetadataCacheEnabled() {
	return process.env.NODE_ENV !== "development";
}
const routeFunctionCache = /* @__PURE__ */ new WeakMap();
const USE_CACHE_FUNCTION_SYMBOL = Symbol.for("vinext.useCacheFunction");
const CACHE_HEADERS = {
	noCache: "no-cache, no-store",
	revalidate: "public, max-age=0, must-revalidate"
};
function isObject(value) {
	return typeof value === "object" && value !== null;
}
function readFunction(module, key) {
	if (!module) return null;
	const value = Reflect.get(module, key);
	if (typeof value !== "function") return null;
	const fn = (props) => Reflect.apply(value, module, [props]);
	if (Reflect.get(value, USE_CACHE_FUNCTION_SYMBOL) === true) Reflect.set(fn, USE_CACHE_FUNCTION_SYMBOL, true);
	return fn;
}
function isSitemapEntries(value) {
	return Array.isArray(value);
}
function isRobotsConfig(value) {
	return isObject(value) && !Array.isArray(value);
}
function isManifestConfig(value) {
	return isObject(value) && !Array.isArray(value);
}
function isImageMetadataRoute(route) {
	return route.type === "icon" || route.type === "apple-icon" || route.type === "opengraph-image" || route.type === "twitter-image";
}
function metadataRouteCacheHeader(route) {
	if (route.isDynamic && isImageMetadataRoute(route) && process.env.NODE_ENV === "development") return CACHE_HEADERS.noCache;
	return CACHE_HEADERS.revalidate;
}
function withMetadataRouteCacheHeader(response, route) {
	const headers = new Headers(response.headers);
	if (!headers.has("Cache-Control")) headers.set("Cache-Control", metadataRouteCacheHeader(route));
	return new Response(response.body, {
		headers,
		status: response.status,
		statusText: response.statusText
	});
}
function getMetadataRouteFunctions(route) {
	const cached = routeFunctionCache.get(route);
	if (cached) return cached;
	const generateImageMetadata = route.isDynamic && isImageMetadataRoute(route) ? readFunction(route.module, "generateImageMetadata") : null;
	const functions = {
		defaultExport: route.isDynamic ? readFunction(route.module, "default") : null,
		generateImageMetadata,
		generateSitemaps: route.type === "sitemap" && route.isDynamic ? readFunction(route.module, "generateSitemaps") : null,
		hasGeneratedImageMetadata: route.isDynamic && isImageMetadataRoute(route) && Boolean(generateImageMetadata)
	};
	routeFunctionCache.set(route, functions);
	return functions;
}
function isUseCacheFunction(value) {
	return value !== null && Reflect.get(value, USE_CACHE_FUNCTION_SYMBOL) === true;
}
/**
* Enumerate metadata URLs whose default export is an explicit public
* `"use cache"` function. These are safe to invoke during prerendering and the
* resulting response can be seeded as an App Route artifact.
*/
async function getPrerenderableMetadataRoutePaths(metadataRoutes) {
	const paths = [];
	for (const route of metadataRoutes) {
		if (!route.isDynamic || route.servedUrl.includes("[")) continue;
		const functions = getMetadataRouteFunctions(route);
		if (!isUseCacheFunction(functions.defaultExport)) continue;
		if (route.type !== "sitemap" || !functions.generateSitemaps) {
			paths.push({
				path: route.servedUrl,
				routePattern: route.servedUrl,
				routeSegments: route.routeSegments ?? []
			});
			continue;
		}
		const entries = await functions.generateSitemaps({});
		if (!Array.isArray(entries)) continue;
		const sitemapPrefix = route.servedUrl.slice(0, -4);
		for (const entry of entries) {
			if (!isObject(entry) || Reflect.get(entry, "id") == null) throw new Error("id property is required for every item returned from generateSitemaps");
			const id = String(Reflect.get(entry, "id"));
			if (!id || id.includes("/")) continue;
			paths.push({
				path: `${sitemapPrefix}/${encodeURIComponent(id)}.xml`,
				routePattern: route.servedUrl,
				routeSegments: route.routeSegments ?? []
			});
		}
	}
	return paths;
}
function getCachedMetadataRouteValue(entry) {
	const value = entry?.value.value;
	if (!value || value.kind !== "APP_ROUTE") return null;
	return value.headers["x-vinext-metadata-route-cache"] === "1" ? value : null;
}
function buildCachedMetadataRouteResponse(value, options) {
	return buildRouteHandlerCachedResponse(value, {
		...options,
		isHead: false
	});
}
function markMetadataRouteCacheValue(value) {
	value.headers[VINEXT_METADATA_ROUTE_CACHE_HEADER] = "1";
	return value;
}
function buildMetadataRouteTags(route, cleanPathname, collectedTags) {
	return buildPageCacheTags(cleanPathname, collectedTags, route.routeSegments ?? [], "route");
}
function captureRenderedMetadataRoute(response) {
	const cacheLife = _consumeRequestScopedCacheLife();
	const collectedTags = getCollectedFetchTags();
	if (process.env.VINEXT_PRERENDER === "1") {
		applyPrerenderCacheLifeHeader(response.headers, cacheLife);
		applyPrerenderCacheTagsHeader(response.headers, collectedTags);
	}
	return {
		cacheLife,
		collectedTags,
		response
	};
}
async function writeRenderedMetadataRoute(options, route, routeKey, rendered, previousEntry) {
	if (!options.isrSet || !rendered.response.ok || !isMetadataResponseCacheable(rendered.response)) return;
	const previousCacheControl = previousEntry?.value.cacheControl;
	const defaultCacheLife = cacheLifeProfiles.default;
	const revalidate = rendered.cacheLife?.revalidate ?? previousCacheControl?.revalidate ?? defaultCacheLife.revalidate ?? 900;
	const expire = rendered.cacheLife?.expire ?? previousCacheControl?.expire ?? defaultCacheLife.expire;
	const stale = resolveClientStaleTimeSeconds(rendered.cacheLife) ?? previousCacheControl?.stale;
	const value = markMetadataRouteCacheValue(await buildAppRouteCacheValue(rendered.response));
	await options.isrSet(routeKey, value, {
		cacheControl: isrCacheControl(revalidate, {
			...typeof expire === "number" ? { expireSeconds: expire } : {},
			...typeof stale === "number" ? { staleSeconds: stale } : {}
		}),
		tags: buildMetadataRouteTags(route, options.cleanPathname, rendered.collectedTags)
	});
}
async function runMetadataRouteRegeneration(options, route, render, routeKey, previousEntry, executionContext) {
	const requestContext = createRequestContext({ executionContext });
	try {
		await runWithRequestContext(requestContext, async () => {
			ensureFetchPatch();
			setCurrentFetchSoftTags(buildMetadataRouteTags(route, options.cleanPathname, []));
			const rendered = await render();
			if (rendered) await writeRenderedMetadataRoute(options, route, routeKey, rendered, previousEntry);
		});
	} finally {
		await closeAfterResponse(requestContext);
	}
}
async function readMatchedPrerenderedMetadataRouteResponse(options, route, functions, render) {
	if (!isOuterMetadataCacheEnabled() || !options.isrGet || !options.isrRouteKey) return {
		cachedEntry: null,
		response: null
	};
	const routeKey = options.isrRouteKey(options.cleanPathname);
	let cached;
	try {
		cached = await options.isrGet(routeKey);
	} catch (error) {
		console.error("[vinext] Metadata route ISR cache read error:", error);
		return {
			cachedEntry: null,
			response: null
		};
	}
	const value = getCachedMetadataRouteValue(cached);
	if (!cached || !value) return {
		cachedEntry: null,
		response: null
	};
	if (cached.isExpired) return {
		cachedEntry: cached,
		response: null
	};
	const revalidate = cached.value.cacheControl?.revalidate;
	const revalidateSeconds = typeof revalidate === "number" ? revalidate : Infinity;
	if (!cached.isStale) return {
		cachedEntry: cached,
		response: buildCachedMetadataRouteResponse(value, {
			cacheControl: cached.value.cacheControl,
			cacheState: "HIT",
			revalidateSeconds
		})
	};
	if (isUseCacheFunction(functions.defaultExport) && options.isrSet && options.scheduleBackgroundRegeneration) {
		const executionContext = getRequestExecutionContext();
		options.scheduleBackgroundRegeneration(routeKey, () => runMetadataRouteRegeneration(options, route, render, routeKey, cached, executionContext), {
			routerKind: "App Router",
			routePath: route.servedUrl,
			routeType: "route"
		});
	}
	return {
		cachedEntry: cached,
		response: buildCachedMetadataRouteResponse(value, {
			cacheControl: cached.value.cacheControl,
			cacheState: "STALE",
			revalidateSeconds
		})
	};
}
function isGeneratedSitemapPath(route, cleanPathname) {
	const prefix = route.servedUrl.slice(0, -4);
	if (!cleanPathname.startsWith(`${prefix}/`) || !cleanPathname.endsWith(".xml")) return false;
	const id = cleanPathname.slice(prefix.length + 1, -4);
	return id.length > 0 && !id.includes("/");
}
function matchMetadataRoute(route, cleanPathname, functions, getUrlParts) {
	if (route.patternParts) {
		const urlParts = getUrlParts();
		if (functions.hasGeneratedImageMetadata && urlParts.length > 0) {
			const params = matchMetadataRoutePattern(urlParts.slice(0, -1), route.patternParts);
			if (params) return {
				params,
				imageId: urlParts[urlParts.length - 1]
			};
		}
		const params = matchMetadataRoutePattern(urlParts, route.patternParts);
		return params ? {
			params,
			imageId: null
		} : null;
	}
	if (functions.hasGeneratedImageMetadata && cleanPathname.startsWith(`${route.servedUrl}/`)) {
		const imageSuffix = cleanPathname.slice(route.servedUrl.length + 1);
		if (!imageSuffix || imageSuffix.includes("/")) return null;
		return {
			params: Object.create(null),
			imageId: imageSuffix
		};
	}
	return cleanPathname === route.servedUrl ? {
		params: null,
		imageId: null
	} : null;
}
function findGeneratedSitemapId(entries, rawId) {
	if (!Array.isArray(entries)) return null;
	for (const entry of entries) {
		if (!isObject(entry) || Reflect.get(entry, "id") == null) throw new Error("id property is required for every item returned from generateSitemaps");
		const id = Reflect.get(entry, "id");
		if (String(id) === rawId) return rawId;
	}
	return null;
}
function makeThenableMetadataRouteId(id) {
	return Object.assign(Promise.resolve(id), {
		toString() {
			return id;
		},
		valueOf() {
			return id;
		},
		[Symbol.toPrimitive]() {
			return id;
		}
	});
}
async function handleGeneratedSitemap(route, cleanPathname, functions) {
	if (!functions.generateSitemaps || !functions.defaultExport) return null;
	const sitemapPrefix = route.servedUrl.slice(0, -4);
	if (!cleanPathname.startsWith(`${sitemapPrefix}/`) || !cleanPathname.endsWith(".xml")) return null;
	const rawId = cleanPathname.slice(sitemapPrefix.length + 1, -4);
	if (rawId.includes("/")) return null;
	const matchedId = findGeneratedSitemapId(await functions.generateSitemaps({}), rawId);
	if (!matchedId) return notFoundResponse();
	const result = await functions.defaultExport({ id: makeThenableMetadataRouteId(matchedId) });
	if (result instanceof Response) return withMetadataRouteCacheHeader(result, route);
	if (!isSitemapEntries(result)) throw new TypeError("Metadata sitemap routes must return an array.");
	return markFullyBufferedBody(new Response(sitemapToXml(result), { headers: {
		"Content-Type": route.contentType,
		"Cache-Control": metadataRouteCacheHeader(route)
	} }));
}
function findGeneratedImageId(imageMetadata, imageId, servedUrl) {
	if (!Array.isArray(imageMetadata)) return null;
	for (const item of imageMetadata) {
		if (!isObject(item) || Reflect.get(item, "id") == null) throw new Error("id property is required for every item returned from generateImageMetadata");
		const itemId = String(Reflect.get(item, "id"));
		if (!isValidMetadataImageId(itemId)) {
			console.warn(`[vinext] Skipping metadata route ${servedUrl} image id "${itemId}" because metadata image ids must match /^[a-zA-Z0-9-_.]+$/.`);
			continue;
		}
		if (itemId === imageId) return itemId;
	}
	return null;
}
async function callDynamicMetadataRoute(route, match, makeThenableParams, functions) {
	if (!functions.defaultExport) {
		console.warn(`[vinext] Dynamic metadata route ${route.servedUrl} has no default export.`);
		return notFoundResponse();
	}
	const paramsThenable = makeThenableParams(match.params ?? {});
	let result;
	if (functions.hasGeneratedImageMetadata) {
		if (match.imageId === null || !isValidMetadataImageId(match.imageId)) return notFoundResponse();
		if (!functions.generateImageMetadata) return notFoundResponse();
		const matchedImageId = findGeneratedImageId(await functions.generateImageMetadata({ params: paramsThenable }), match.imageId, route.servedUrl);
		if (!matchedImageId) return notFoundResponse();
		result = await functions.defaultExport({
			params: paramsThenable,
			id: makeThenableMetadataRouteId(matchedImageId)
		});
	} else result = await functions.defaultExport({ params: paramsThenable });
	if (result instanceof Response) return withMetadataRouteCacheHeader(result, route);
	let body;
	if (route.type === "sitemap") {
		if (!isSitemapEntries(result)) throw new TypeError("Metadata sitemap routes must return an array.");
		body = sitemapToXml(result);
	} else if (route.type === "robots") {
		if (!isRobotsConfig(result)) throw new TypeError("Metadata robots routes must return an object.");
		body = robotsToText(result);
	} else if (route.type === "manifest") {
		if (!isManifestConfig(result)) throw new TypeError("Metadata manifest routes must return an object.");
		body = manifestToJson(result);
	} else if (isImageMetadataRoute(route)) throw new TypeError(`Dynamic metadata ${route.type} route ${route.servedUrl} must return a Response.`);
	else body = JSON.stringify(result);
	return markFullyBufferedBody(new Response(body, { headers: {
		"Content-Type": route.contentType,
		"Cache-Control": metadataRouteCacheHeader(route)
	} }));
}
function serveStaticMetadataRoute(route) {
	if (typeof route.fileDataBase64 !== "string") throw new Error(`[vinext] Static metadata route ${route.servedUrl} is missing embedded file data.`);
	try {
		const binary = atob(route.fileDataBase64);
		const bytes = new Uint8Array(binary.length);
		for (let index = 0; index < binary.length; index++) bytes[index] = binary.charCodeAt(index);
		return markFullyBufferedBody(new Response(bytes, { headers: {
			"Content-Type": route.contentType,
			"Cache-Control": metadataRouteCacheHeader(route)
		} }));
	} catch (error) {
		const reason = error instanceof Error && error.message ? `: ${error.message}` : "";
		throw new Error(`[vinext] Failed to decode embedded metadata route file data for ${route.servedUrl}${reason}`, { cause: error });
	}
}
function isMetadataRouteRequestPath(metadataRoutes, cleanPathname) {
	let urlParts;
	const getUrlParts = () => urlParts ??= cleanPathname.split("/").filter(Boolean);
	for (const route of metadataRoutes) {
		const functions = getMetadataRouteFunctions(route);
		if (route.type === "sitemap" && route.isDynamic && functions.generateSitemaps && isGeneratedSitemapPath(route, cleanPathname)) return true;
		if (matchMetadataRoute(route, cleanPathname, functions, getUrlParts)) return true;
	}
	return false;
}
async function writeMetadataRouteMiss(options, route, functions, rendered, cachedEntry) {
	if (process.env.VINEXT_PRERENDER === "1" || !isOuterMetadataCacheEnabled() || !rendered.response.ok || !isMetadataResponseCacheable(rendered.response) || !isUseCacheFunction(functions.defaultExport) || !options.isrRouteKey || !options.isrSet) return;
	try {
		await writeRenderedMetadataRoute(options, route, options.isrRouteKey(options.cleanPathname), {
			...rendered,
			response: rendered.response.clone()
		}, cachedEntry);
	} catch (error) {
		console.error("[vinext] Metadata route ISR cache write error:", error);
	}
}
async function handleMetadataRouteRequest(options) {
	let urlParts;
	const getUrlParts = () => urlParts ??= options.cleanPathname.split("/").filter(Boolean);
	for (const route of options.metadataRoutes) {
		const functions = getMetadataRouteFunctions(route);
		if (route.type === "sitemap" && route.isDynamic) {
			if (functions.generateSitemaps) {
				if (isGeneratedSitemapPath(route, options.cleanPathname)) {
					const render = async () => {
						setCurrentFetchSoftTags(buildMetadataRouteTags(route, options.cleanPathname, []));
						const response = await handleGeneratedSitemap(route, options.cleanPathname, functions);
						return response ? captureRenderedMetadataRoute(response) : null;
					};
					const cached = await readMatchedPrerenderedMetadataRouteResponse(options, route, functions, render);
					if (cached.response) return cached.response;
					const rendered = await render();
					if (rendered) {
						await writeMetadataRouteMiss(options, route, functions, rendered, cached.cachedEntry);
						return rendered.response;
					}
				}
				continue;
			}
		}
		const match = matchMetadataRoute(route, options.cleanPathname, functions, getUrlParts);
		if (!match) continue;
		const render = async () => {
			setCurrentFetchSoftTags(buildMetadataRouteTags(route, options.cleanPathname, []));
			return captureRenderedMetadataRoute(route.isDynamic ? await callDynamicMetadataRoute(route, match, options.makeThenableParams, functions) : serveStaticMetadataRoute(route));
		};
		const cached = await readMatchedPrerenderedMetadataRouteResponse(options, route, functions, render);
		if (cached.response) return cached.response;
		const rendered = await render();
		await writeMetadataRouteMiss(options, route, functions, rendered, cached.cachedEntry);
		return rendered.response;
	}
	return null;
}
//#endregion
export { getPrerenderableMetadataRoutePaths, handleMetadataRouteRequest, isMetadataRouteRequestPath };
