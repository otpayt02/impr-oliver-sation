import { VINEXT_PRERENDER_SECRET_HEADER } from "../utils/protocol-headers.js";
import { VINEXT_PRERENDER_METADATA_ROUTES_PATH, VINEXT_PRERENDER_PAGES_STATIC_PATHS_PATH, VINEXT_PRERENDER_READINESS_HEADER, VINEXT_PRERENDER_READINESS_PATH, VINEXT_PRERENDER_STATIC_PARAMS_PATH } from "./headers.js";
//#region src/server/worker-prerender-discovery.ts
const PRERENDER_DISCOVERY_PATHS = /* @__PURE__ */ new Set([
	VINEXT_PRERENDER_STATIC_PARAMS_PATH,
	VINEXT_PRERENDER_PAGES_STATIC_PATHS_PATH,
	VINEXT_PRERENDER_METADATA_ROUTES_PATH,
	VINEXT_PRERENDER_READINESS_PATH
]);
function isWorkerPrerenderDiscoveryPath(pathname) {
	return PRERENDER_DISCOVERY_PATHS.has(pathname);
}
function workerCapabilityMatches(provided, expected) {
	let mismatch = provided.length ^ expected.length;
	for (let index = 0; index < expected.length; index++) mismatch |= (provided.charCodeAt(index) || 0) ^ expected.charCodeAt(index);
	return mismatch === 0;
}
/**
* Respond to an authenticated staged-version readiness request without
* invoking middleware, routing, or rendering. The CDN adapter validates the
* expected Worker version before callers return this response.
*/
function createWorkerPrerenderReadinessResponse(ctx, request) {
	if (!request.url.includes("/__vinext/prerender/readiness")) return null;
	if (new URL(request.url).pathname !== "/__vinext/prerender/readiness") return null;
	if (ctx.isPrerenderPathDiscovery !== true || request.method !== "GET" && request.method !== "POST" || !request.headers.has("X-Vinext-Expected-Worker-Version")) return new Response(null, {
		status: 404,
		headers: { "Cache-Control": "no-store" }
	});
	return new Response(null, {
		status: 204,
		headers: {
			"Cache-Control": "no-store",
			[VINEXT_PRERENDER_READINESS_HEADER]: "1"
		}
	});
}
/**
* Mark only a request carrying the current build's capability as an internal
* path-discovery request. The capability is compiled into the Worker entry and
* rotated on every build; the public version override proves which staged
* version Cloudflare invoked but is deliberately not treated as authorization.
*/
function createWorkerPrerenderDiscoveryContext(base, request, expectedSecret) {
	const providedSecret = request.headers.get(VINEXT_PRERENDER_SECRET_HEADER);
	if (!expectedSecret || !providedSecret || !workerCapabilityMatches(providedSecret, expectedSecret)) return base;
	const pathname = new URL(request.url).pathname;
	if (!isWorkerPrerenderDiscoveryPath(pathname)) return base;
	return {
		...base,
		isPrerenderPathDiscovery: true
	};
}
//#endregion
export { createWorkerPrerenderDiscoveryContext, createWorkerPrerenderReadinessResponse, isWorkerPrerenderDiscoveryPath, workerCapabilityMatches };
