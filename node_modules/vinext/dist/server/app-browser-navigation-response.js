import { stripBasePath } from "../utils/base-path.js";
//#region src/server/app-browser-navigation-response.ts
function shouldRecoverSamePathSearchCommitOnResponseCompletion(options) {
	if (!options.programmaticTransition || options.navigationKind !== "navigate") return false;
	return options.currentSnapshot.pathname === stripBasePath(options.targetUrl.pathname, options.basePath) && options.currentSnapshot.search !== options.targetUrl.search;
}
//#endregion
export { shouldRecoverSamePathSearchCommitOnResponseCompletion };
