import { canonicalizeLoadingShellRscRequestHeaders, canonicalizePrewarmableRscRequestHeaders, createCanonicalRscRequestUrl, createRscRequestUrl } from "../../server/app-rsc-cache-busting.js";
//#region src/shims/internal/app-prefetch-rsc-request.ts
/**
* Resolve the RSC request identity shared by `<Link>` and `router.prefetch()`.
* Only full routes and ordinary loading shells without request-specific
* context can reuse deploy-prewarmed CDN entries. Contextual requests retain
* their varying headers and deterministic `_rsc` digest.
*/
async function resolveAppPrefetchRscRequest({ canUseCanonicalLoadingShell, fullHref, headers, interceptionContext, mountedSlotsHeader, prefetchInlining, requiresRouteTreePrefetch, rewrittenPrefetchHref }) {
	const canUseCanonicalSharedRequest = process.env.__NEXT_CONFIG_OUTPUT !== "export" && process.env.__VINEXT_CANONICAL_RSC_REQUESTS === "1" && interceptionContext === null && mountedSlotsHeader === null && (rewrittenPrefetchHref === null || rewrittenPrefetchHref === fullHref) && !requiresRouteTreePrefetch && !prefetchInlining;
	const usesCanonicalLoadingShell = canUseCanonicalSharedRequest && canUseCanonicalLoadingShell && canonicalizeLoadingShellRscRequestHeaders(headers);
	const usesCanonicalFullRoute = canUseCanonicalSharedRequest && !usesCanonicalLoadingShell && canonicalizePrewarmableRscRequestHeaders(headers);
	const [rscUrl, ...additionalRscUrls] = await Promise.all([usesCanonicalFullRoute ? createCanonicalRscRequestUrl(fullHref) : createRscRequestUrl(fullHref, headers), ...rewrittenPrefetchHref !== null && rewrittenPrefetchHref !== fullHref ? [createRscRequestUrl(rewrittenPrefetchHref, headers)] : []]);
	return {
		additionalRscUrls,
		rscUrl,
		usesCanonicalPrewarmedRequest: usesCanonicalLoadingShell || usesCanonicalFullRoute
	};
}
//#endregion
export { resolveAppPrefetchRscRequest };
