//#region src/shims/cacheability-classification.d.ts
declare const CACHEABILITY_REQUEST_STATE: unique symbol;
declare const CACHEABILITY_POLICY_HEADERS: readonly ["cache-control", "cdn-cache-control", "cloudflare-cdn-cache-control"];
type CacheabilityPolicyHeader = (typeof CACHEABILITY_POLICY_HEADERS)[number];
type RouteCacheabilityOutcome = {
  cacheControl?: string;
  cacheable: boolean;
  classificationFailure?: boolean;
  dynamicUsage?: boolean;
  reason?: string;
  tags?: readonly string[];
};
type RouteCacheabilityState = {
  admission?: {
    manifest?: unknown;
    policy: "deny" | "manifest" | "runtime";
    representation?: string;
    requestKey?: string;
    routePathname?: string;
  };
  /** Optional admission budget override used by focused runtime tests. */
  captureBudget?: {
    maxBytes: number;
    reservedBytes: number;
  };
  captureDeadlineAt: number;
  complete?: (outcome: RouteCacheabilityOutcome) => void;
  completion?: Promise<RouteCacheabilityOutcome>;
  completedResponseBody?: boolean;
  explicitConfigCachePolicy?: boolean;
  explicitResponseCachePolicy?: boolean;
  finalResponseVetoReason?: string;
  forcedDynamicReason?: string;
  /** A route-config decision that applies to every concrete identity for this pattern. */
  patternDynamicReason?: string;
  frameworkResponseCachePolicy?: Partial<Record<CacheabilityPolicyHeader, string>>;
  mode: "admit" | "identity" | "probe";
  outcome?: RouteCacheabilityOutcome;
  preserveResponseCachePolicy?: boolean;
  /** Cache-key behavior declared by the active CDN adapter. */
  responseVary?: "verbatim";
  probeBailout?: {
    kind: "private-cache";
    outcome: RouteCacheabilityOutcome;
  };
  route?: {
    kind: "app-page" | "app-route" | "pages-page";
    pattern: string;
  };
};
/** Preserve the existing policy when hybrid routing hands the request to Pages Router. */
declare function preserveRouteCacheabilityResponsePolicy(): void;
declare function readRouteCacheabilityState(): RouteCacheabilityState | null;
declare function beginRouteCacheability(kind: "app-page" | "app-route" | "pages-page", pattern: string): boolean;
/** Prevent shared caching when request processing before route dispatch was request-specific. */
declare function markRouteCacheabilityDynamic(reason: string): void;
/** Mark an effective route configuration that makes the whole pattern dynamic. */
declare function markRouteCacheabilityPatternDynamic(reason: string): void;
/** Read a request-specific routing veto without making the route globally dynamic. */
declare function getRouteCacheabilityDynamicReason(): string | null;
/** Reuse the active request's bounded response-capture envelope when available. */
declare function getRouteCacheabilityCaptureOptions(): Pick<RouteCacheabilityState, "captureBudget" | "captureDeadlineAt"> | null;
/** Keep a completed response private without treating it as static-to-dynamic. */
declare function markRouteCacheabilityFinalResponseUncacheable(reason: string): void;
/** Record that next.config explicitly owns the final response cache policy. */
declare function markRouteCacheabilityExplicitConfigPolicy(): void;
/** Record a public cache policy supplied by the Route Handler itself. */
declare function markRouteCacheabilityExplicitResponsePolicy(): void;
/** Record that the response body reached clean EOF and is now a replay stream. */
declare function markRouteCacheabilityResponseBodyComplete(): void;
/** Record framework-owned policy so admission can identify policy added later. */
declare function captureRouteCacheabilityResponsePolicy(headers: Headers): void;
/** True only for an authenticated probe that must render the matched App Page. */
declare function isRouteCacheabilityProbe(): boolean;
/** True when the outer Worker must decide cache admission after clean EOF. */
declare function isRouteCacheabilityEvaluation(): boolean;
/** True for the authenticated routing pass that must not evaluate user components. */
declare function isRouteCacheabilityIdentityProbe(): boolean;
/** True for every side-effect-free staged Worker observation request. */
declare function isStagedCacheabilityProbeActive(): boolean;
declare function deferRouteCacheability(): ((outcome: RouteCacheabilityOutcome) => void) | null;
declare function recordRouteCacheability(outcome: RouteCacheabilityOutcome): void;
declare function recordRouteCacheabilityProbeBailout(kind: "private-cache", outcome: RouteCacheabilityOutcome): void;
//#endregion
export { CACHEABILITY_POLICY_HEADERS, CACHEABILITY_REQUEST_STATE, RouteCacheabilityOutcome, RouteCacheabilityState, beginRouteCacheability, captureRouteCacheabilityResponsePolicy, deferRouteCacheability, getRouteCacheabilityCaptureOptions, getRouteCacheabilityDynamicReason, isRouteCacheabilityEvaluation, isRouteCacheabilityIdentityProbe, isRouteCacheabilityProbe, isStagedCacheabilityProbeActive, markRouteCacheabilityDynamic, markRouteCacheabilityExplicitConfigPolicy, markRouteCacheabilityExplicitResponsePolicy, markRouteCacheabilityFinalResponseUncacheable, markRouteCacheabilityPatternDynamic, markRouteCacheabilityResponseBodyComplete, preserveRouteCacheabilityResponsePolicy, readRouteCacheabilityState, recordRouteCacheability, recordRouteCacheabilityProbeBailout };